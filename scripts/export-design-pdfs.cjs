// Artifact rendering only. Interactive UI verification uses Computer Use.
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');
const path=require('node:path');
(async()=>{const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1280,height:900}});for(const [pageName,out] of [['index.html','02-three-design-concepts.pdf'],['preparation.html','03-content-and-preparation.pdf']]){await page.goto('http://127.0.0.1:4197/designs/'+pageName);await page.locator('img').evaluateAll(xs=>xs.forEach(i=>i.loading='eager'));await page.waitForFunction(()=>[...document.images].every(i=>i.complete&&i.naturalWidth));await page.pdf({path:path.resolve(__dirname,'../output/pdf',out),format:'A4',printBackground:true,preferCSSPageSize:true,displayHeaderFooter:true,headerTemplate:'<span></span>',footerTemplate:'<div style="font:8px sans-serif;width:100%;padding:0 15mm;color:#756c62;display:flex;justify-content:space-between"><span>onlyyuuka / Website proposal</span><span class="pageNumber"></span></div>',margin:{top:'15mm',bottom:'15mm',left:'15mm',right:'15mm'}});console.log(out)}await browser.close()})().catch(e=>{console.error(e);process.exit(1)});
